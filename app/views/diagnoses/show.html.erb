<p style="color: green"><%= notice %></p>

<div class="flex flex-col items-center justify-center">

  <div class='grid grid-rows-auto'>
    <div class='row-start-1 max-h-fit'>
      <%= render 'diagnosis' %>
    </div>

    <div class='row-start-2 h-fit my-3'>
      <%= render "shared/show/edit_btn", path: edit_diagnosis_path(@diagnosis) %>
      <%= render "shared/show/back_btn", path: diagnoses_path %>
      <%= render "shared/show/delete_btn", path: diagnosis_path(@diagnosis) %>
    </div>
  </div>

</div>
