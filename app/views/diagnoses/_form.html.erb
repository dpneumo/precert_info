<%= form_with(model: diagnosis) do |form| %>
  <% if diagnosis.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(diagnosis.errors.count, "error") %> prohibited this diagnosis from being saved:</h2>

      <ul>
        <% diagnosis.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :icd10, style: "display: block" %>
    <%= form.text_field :icd10 %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
