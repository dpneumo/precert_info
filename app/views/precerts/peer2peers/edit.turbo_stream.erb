<%# app/views/precerts/peer2peers/edit.turbo_stream.erb %>

<%= turbo_stream.show_remote_modal do %>
  <dialog id="contact_form_modal"
          aria-labelledby="modal_title">
    <%= render 'shared/modal_header',
                title: 'Edit Peer-to-Peer' %>

    <%= render 'precerts/peer2peers/form',
                precert: @peer2peer.precert,
                peer2peer: @peer2peer,
                method: :patch %>
  </dialog>
<% end %>
