<div class='mx-4 mb-5 min-w-0'>
  <%= form.label attrib, style: 'display: block', class:'form-field-label' %>
  <% values.each do |val| %>
    <%= form.radio_button attrib, val %>
    <%= form.label (attrib.to_s + '_' + val.to_s).to_sym, val %>
  <% end %>
</div>
