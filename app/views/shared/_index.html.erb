<div class='flex flex-col sm:flex-row items-center justify-center'>
  <div class='mb-5 mx-2'>
    <div class="flex flex-row justify-between">
      <h1 class="font-sans font-bold text-2xl"><%= klass.ndx_header %></h1>
    </div>

    <%= render "shared/table",
            head: head,
            row:  row,
            collection: collection %>

    <br>
    <%== pagy_nav(@pagy) %>

    <hr class="mt-1 border-1 border-gray-700" />
    <br>

    <%= link_to klass.newlink_txt, newlink_path, class: "text-lg font-sans font-medium link-hover" %>
  </div>
</div>
