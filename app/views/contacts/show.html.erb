<p style="color: green"><%= notice %></p>

<div class="flex flex-col items-center justify-center">

  <div class='grid grid-rows-auto'>
    <div class='row-start-1 max-h-fit'>
      <%= render 'contact' %>
    </div>

    <div class='row-start-2 h-fit my-3'>
      <%= render "shared/show/edit_btn", path: edit_precert_contact_path(@contact.precert, @contact) %>
      <%= render "shared/show/back_btn", path: precert_path(@contact.precert) %>
      <%= render "shared/show/delete_btn", path: precert_contact_path(@contact.precert, @contact) %>
    </div>
  </div>

</div>
