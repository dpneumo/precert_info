<div class="flex flex-col md:flex-row">
  <%= render 'shared/form/disabled_text_field', attrib: :precert_id, form: form %>
</div>

<div class="flex flex-col md:flex-row">
  <%= render 'shared/form/time_field', attrib: :contact_time, form: form %>
  <%= render 'shared/form/number_field', attrib: :duration, form: form %>
</div>

<div class="flex flex-col md:flex-row">
  <%= render 'shared/form/text_area', attrib: :note, form: form %>
</div>
