<p style="color: green"><%= notice %></p>

<div class="flex flex-col items-center justify-center">

  <div class='grid grid-rows-auto'>
    <div class='row-start-1 max-h-fit'>
      <%= render 'provider' %>
    </div>

    <div class='row-start-2 h-fit my-3'>
      <%= render "shared/show/edit_btn", path: edit_provider_path(@provider) %>
      <%= render "shared/show/back_btn", path: providers_path %>
      <%= render "shared/show/delete_btn", path: provider_path(@provider) %>
    </div>
  </div>

</div>
